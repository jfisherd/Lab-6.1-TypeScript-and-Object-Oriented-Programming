import {Product} from '../models/Product'

// export class TaxedProduct extends Product {
    

//     calculateTax(): Product {
//         this.weight? return this.price * 1.1 : return this.price; // if Product has a weight (a physical product), its price is returned with 
//     }
// }

export function calculateTax(): Product {
        this.weight? this.price = this.price * 1.1 // if Product has a weight (a physical product), its price is returned with 
    }